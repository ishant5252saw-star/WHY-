# ─── Supabase (auto-provisioned by Bolt) ──────────────────────
# These are pre-populated in the Bolt environment. Do NOT change them.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ─── AI Provider (server-side only, set as Supabase Edge Function secret) ──
# Add these in Supabase > Edge Functions > Secrets
# OPENAI_API_KEY=sk-...
# AI_MODEL=gpt-4o-mini
